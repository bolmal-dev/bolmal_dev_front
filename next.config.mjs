/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ticketimage.interpark.com', 'bolmal-buckets.s3.ap-northeast-2.amazonaws.com', 'tkfile.yes24.com'],
    },
};

export default nextConfig;
